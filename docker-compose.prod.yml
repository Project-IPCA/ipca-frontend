services:
  ipca-frontend:
    container_name: ipca-frontend
    build: Dockerfile.prod
    restart: unless-stopped
    networks:
      - ipca-stack
    env_file:
      - .env

networks:
  ipca-stack:
    external: true
